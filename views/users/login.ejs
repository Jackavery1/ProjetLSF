<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>LSF - Connexion</title>
    <%- include('../partials/head') %>
  </head>

  <body>
    <%- include('../partials/navbar') %>

    <main class="min-h-screen bg-[#fff3eb] py-12 px-4">
      <div class="max-w-md mx-auto">
        <div class="bg-white rounded-xl shadow-lg p-8">
          <h1 class="text-3xl font-bold text-[#00a8a3] mb-6 text-center">
            Connexion
          </h1>

          <form action="/api/auth/login" method="POST" class="space-y-6">
            <div>
              <label
                for="email"
                class="block text-sm font-medium text-gray-700 mb-2"
              >
                Email
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#00a8a3] focus:border-transparent outline-none"
                placeholder="votre@email.com"
              />
            </div>

            <div>
              <label
                for="password"
                class="block text-sm font-medium text-gray-700 mb-2"
              >
                Mot de passe
              </label>
              <input
                type="password"
                id="password"
                name="password"
                required
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#00a8a3] focus:border-transparent outline-none"
                placeholder="••••••••"
              />
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
            <div
              class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg"
            >
              <%= error %>
            </div>
            <% } %>

            <button
              type="submit"
              class="w-full bg-[#f46b5e] hover:bg-[#e25b4e] text-white font-semibold py-3 px-4 rounded-lg shadow transition"
            >
              Se connecter
            </button>
          </form>

          <div class="mt-6 text-center text-sm text-gray-600">
            <p>
              Pas encore de compte ?
              <a href="/register" class="text-[#00a8a3] hover:underline"
                >Créer un compte</a
              >
            </p>
          </div>
        </div>
      </div>
    </main>

    <%- include('../partials/footer') %>
  </body>
</html>

