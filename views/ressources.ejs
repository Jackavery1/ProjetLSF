<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Import Tailwind CSS -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <!-- Import d'une police similaire à Open Sans -->
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap"
      rel="stylesheet"
    />
    <title>Document</title>
  </head>
  <body>
    <!-- Exemple de liste stylisée pour ressources.ejs ou quizz.ejs -->
    <section class="max-w-3xl mx-auto my-12">
      <h2 class="text-3xl font-bold text-center text-[#00a8a3] mb-8">
        📚 Ressources utiles
      </h2>
      <ul class="space-y-6">
        <% ressources.forEach(ressource => { %>
        <li
          class="bg-white rounded-xl shadow hover:shadow-lg transition p-6 flex flex-col md:flex-row md:items-center md:justify-between gap-4"
        >
          <div>
            <h3 class="text-xl font-semibold text-[#f46b5e] mb-1">
              <%= ressource.titre %>
            </h3>
            <p class="text-gray-700 mb-2"><%= ressource.description %></p>
          </div>
          <a
            href="<%= ressource.lien %>"
            target="_blank"
            class="inline-block bg-[#00a8a3] hover:bg-[#00928e] text-white px-5 py-2 rounded-lg shadow font-medium transition"
          >
            Accéder
          </a>
        </li>
        <% }) %>
      </ul>
    </section>
  </body>
</html>
